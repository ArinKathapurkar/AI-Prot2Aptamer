{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "cd api-deployment && pip install -r requirements.txt",
      "watchPatterns": ["api-deployment/**"]
    },
    "deploy": {
      "startCommand": "cd api-deployment && python api.py",
      "restartPolicyType": "ON_FAILURE"
    }
  }